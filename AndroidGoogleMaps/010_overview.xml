<section id="maps" role="wrapper">
	<title>Google Maps</title>
	<section id="maps_mapsview">
		<title>MapsView</title>
		<para>
			Google provides via Google play a
			library for using Google Maps
			in your application. The following description is based on the Google
			Maps Android API v2 which provides significant improvements to the
			older API version.

		</para>
		<para>
			The library provides the
			<code>com.google.android.gms.maps.MapFragment</code>
			class and the
			<code>MapView</code>
			class for displaying the map component.
		</para>
		<para>
			You need to add additional
			information to your
			<filename>AndroidManifest.xml</filename>
			file to use Google Maps.
		</para>

		<para>
			<programlisting>
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="./examples/maps/uses.txt" />
			</programlisting>
		</para>
	</section>

	<section id="maps_fragment">
		<title>MapFragment</title>
		<para>
			The
			<code>MapFragment</code>
			class
			extends the
			<code>Fragment</code>
			class and provides the
			life cycle management and the services for
			displaying a
			<code>GoogleMap</code>
			widget.
			<code>GoogleMap</code>
			is the class which shows the map. The
			<code>MapFragment</code>
			has the
			<code>getMap()</code>
			method to access this class.
		</para>
		<para>
			the
			<code>LatLng</code>
			class can be used to interact with the
			<code>GoogleView</code>
			class.
		</para>

	</section>


	<section id="maps_markers">
		<title>Markers</title>
		<para>
			You can create markers on the map via the
			<code>Marker</code>
			class. This class can be highly customized.
		</para>
		<para>The following code shows an example.</para>
		<para>
			<programlisting language="java">
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="./examples/markers/Marker.java" />
			</programlisting>
		</para>
		<para>
			On the
			<code>GoogleMap</code>
			you can register a listener for the
			markers in your map via the
			<code>setOnMarkerClickListener(OnMarkerClickListener)</code>
			method. The
			<code>OnMarkerClickListener</code>
			class defines the
			<code>onMarkerClicked(Marker)</code>
			method which is called if a marker is clicked.
		</para>
		<para>Similar to you also listen to drag events and info window
			clicks.
		</para>
	</section>

	<section id="maps_changingmapview">
		<title>Changing the GoogleView</title>
		<para>
			The
			<code>GoogleMap</code>
			can be highly customized.
		</para>
		<para>The following example code is taken from the official Google
			webpage.
		</para>
		<para>
			<programlisting language="java">
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="./examples/googlemap/adjust.java" />
			</programlisting>
		</para>


	</section>



	<section id="maps_device">
		<title>Android emulator and Google Maps</title>
		<para> Ensure you create an Google API based emulator. This emulator
			can also be used to test Google map and other Google Play Service
			integration.
		</para>

	</section>




</section>

