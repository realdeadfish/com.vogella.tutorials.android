<section id="exercise_debugging" role="wrapper">
	<title>Exercise: Debugging
	<section id="exercise_debugging1">
		<title>Import example project
		
			Download the zip file from
			http://www.vogella.com/download/androidtest/countries.zip and import
			the Android project from
			into your
			workspace via the
			<menuchoice>
				<guimenu>File</guimenu>
				<guisubmenu>Import...</guisubmenu>
				<guisubmenu>General</guisubmenu>
				<guisubmenu>Existing Projects into Workspace</guisubmenu>
			</menuchoice>
			menu path.
		


	
	<section id="exercise_debugging2">
		<title>Debug the creation of the list items
		After launching the application you suppose to see a list of
			countries registered
			in the system. There is a bug in the
			implementation which causes the
			list to show
			empty strings.
		
		<orderedlist>
			<listitem>
				Launch the application normally.
			</listitem>
			<listitem>
				Realizing that their is a problem, you switch Debug mode on in
					the DDMS
					perspective.
				
			</listitem>
			<listitem>
				Put a breakpoint in MainActivity.onCreate(Bundle
					savedInstanceState) and
					re-launch the activity.
				
			</listitem>
			<listitem>
				 Execute CountryUtils.getCountries() statement and check the
					value of
					"countries" variable. If you believe the value is
					incorrect, use "Step into"
					debug function to find the issue.
				
			</listitem>
			<listitem>
				Fix the issue.
				
			</listitem>
			<listitem>
				Launch the application and make sure the list of countries is
					shown correctly.
				
			</listitem>
		</orderedlist>
	

	<section id="exercise_debugging4">
		<title>Debug list item click behavior
		If you click an item in the list, application will show a toast
			message with the
			name of the country clicked. There is a bug in the
			applications which
			makes this
			function not working properly.
		
		<orderedlist>
			<listitem>
				Launch the application normally.
			</listitem>
			<listitem>
				(Try to) debug MainActivity.onListItemClick() method and fix
					the issue.
				
			</listitem>
			<listitem>
				Launch the application and make sure correct item is show in
					the toast.
				
			</listitem>
		</orderedlist>
	

