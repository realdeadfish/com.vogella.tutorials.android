<section id="preferencesui">
	<title>User interface for preferences</title>
	<section id="preferences_activityvalues">
		<title>Preference resource files</title>
		<para>
			Android allows you to create XML resource files which describes
			preference key-values. Based on this XML file an instance of
			<code>PreferenceActivity</code>
			or
			<code>PreferenceFragment</code>
			can be used to generate an user interface for entering and saving the
			preference values. These objects
			store the key-value pairs
			automatically.
		</para>
		<para>
			To create a preference resource file, select
			<menuchoice>
				<guimenu>File</guimenu>
				<guisubmenu>New</guisubmenu>
				<guisubmenu>Other...</guisubmenu>
				<guisubmenu>Android</guisubmenu>
				<guisubmenu>Android XML File</guisubmenu>
			</menuchoice>
			and select the
			<parameter>Preference</parameter>
			as
			<guilabel>Resource Type</guilabel>
			.
		</para>
	</section>
	<section id="preferences_activity">
		<title>PreferenceFragment</title>
		<para>
			Android provides the
			<code>PreferenceFragment</code>
			class which simplifies the creation of an user interface for
			maintaining preference values. This fragment can load an XML
			preference
			definition file
			via the method
			<code>addPreferencesFromResource()</code>
			.
		</para>


	</section>

</section>