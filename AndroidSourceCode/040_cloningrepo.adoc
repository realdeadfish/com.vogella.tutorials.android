== Cloning Android source code
	
After you installed the repo tool you can get the Android source
code with the following command.
	
[source,txt]
----
include::res/getsource.txt[]
----
	
The command "git branch -a" show you all available branches,
e.g. froyo and gingerbread. You can checkout them via "git checkout
branch_name".
	
To compile the code run the following command.
	
[source,txt]
----
include::res/compile.txt[]
----	

