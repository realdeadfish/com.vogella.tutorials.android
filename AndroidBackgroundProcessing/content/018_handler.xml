<section id="concurrency_handler" role="wrapper">
	<title>Handler</title>
	<section id="concurrency_handler1">
		<title>Purpose of the Handler class</title>
		<para>
			The
			<code>Handler</code>
			class can be used to register to a thread and provides a simple
			channel to send data to this thread.
		</para>
		<para>
			A
			<code>Handler</code>
			object registers itself with the thread in which
			it is created. For
			example, if you create a new
			instance of the
			<code>Handler</code>
			class in the
			<code>onCreate()</code>
			method of your
			<wordasword>activity</wordasword>
			, the resulting
			<code>Handler</code>
			object can be used to post data to the main thread.
		</para>
		<para>

			The data which can be posted via the
			<code>Handler</code>
			class can be an instance of the
			<code>Message</code>
			or the
			<code>Runnable</code>
			class.
		</para>
		<tip>
			<para>A Handler is particular useful if you have want to post
				multiple times data to the main thread.
			</para>
		</tip>
	</section>
	<section id="concurrency_handler2">
		<title>Creating and reusing instances of Handlers</title>
		<para>
			To
			use a handler you have to
			subclass it and override the
			<code>handleMessage()</code>
			method
			to process messages.
		</para>

		<para>
			Your thread can post messages via the
			<code>sendMessage(Message)</code>
			method
			or via the
			<code>sendEmptyMessage()</code>
			method to the
			<code>Handler</code>
			object.
		</para>
		<para>
			To
			process a
			<code>Runnable</code>
			you can use the
			<code>post()</code>
			method.
		</para>
		<para>
			To avoid object creation you can also reuse the existing
			<code>Handler</code>
			object of your
			<wordasword>activity</wordasword>
			.
		</para>
		<para>
			<programlisting language="java">
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="../examples/handler/getActivityHandler.java" />
			</programlisting>
		</para>

		<para>
			The
			<code>View</code>
			class allows you to post objects of type
			<code>Runnable</code>
			via the
			<code>post()</code>
			method.
		</para>
	</section>
	<section id="concurrency_handler3">
		<title>Example</title>
		<para>
			The following code demonstrates the usage of a
			<code>Handler</code>
			via a
			<code>View</code>
			.
		</para>
		<para>
			Assume your
			<wordasword>activity</wordasword>
			uses the following layout.
		</para>
		<para>
			<programlisting>
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="../examples/handler/main.xml" />
			</programlisting>
		</para>
		<para>
			With the following the
			<code>ProgressBar</code>
			get updated once the users presses the
			<code>Button</code>
			.
		</para>
		<para>
			<programlisting>
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="../examples/handler/ProgressTestActivity.java" />
			</programlisting>
		</para>
	</section>
</section>